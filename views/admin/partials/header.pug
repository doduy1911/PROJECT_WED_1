header.header 
    .container-fluid
        .row.align.items-center     
            .col-3 
                .inner-logo 
                    a(href=`/${prefix}/dashboard`) admin 
            .col-9 
                div(class="text-right")
                    a(href=`/${prefix}/auth/logout`
                    class="btn btn-danger btn-sm") Đăng Xuất