mixin search(keyword)
    form(action="" id="form-search") 
        .input-group 
            input(type="text", class="form-control",placeholder="Nhập Từ Khóa",name="keyword",value=keyword)
            .input-group-append 
            button(class="btn btn-success",type="submit") Seach
